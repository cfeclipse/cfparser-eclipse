distro.name=cfparser-eclipse
build.type=release
src.dir=${dist.root.dir}
dist.dir=../dist/${build.type}
temp.dir=./temp
war.contextpath=/
war.name=${distro.name}
war.target.dir=${dist.dir}/${war.name}.war
cfdistro.build.file=${user.home}/cfdistro/build.xml
whatsnew.milestone.id=1

site.suffix=
generator.suffix=R
sc.cfparser-eclipse.type=git
sc.cfparser-eclipse.dest=${src.dir}
sc.cfparser-eclipse.uri=https://github.com/cfeclipse/cfparser-eclipse.git
sc.cfparser-eclipse.branch=master
sc.cfparser-eclipse.remote=origin

generator.lastModified.format='${generator.suffix}'yyyyMMddHHmm
